# Jetson USB camera one-click startup configuration.
# Paths may use ${HOME}; this file is sourced by start_detector.sh.

ROS_DISTRO=foxy
VENV_PATH="${HOME}/venvs/yolo_ros2"

# Refreshed 11-class YOLO26m model. Legacy three-class models remain available
# in weights/manifest.json for controlled rollback or comparison.
MODEL_ID=yolo26m_tabletop_v1
# Keep best.pt until a TensorRT engine has been generated on this exact Jetson.
# Then set this to best.engine; engines must not be copied between devices.
MODEL_FILE=best.pt

# Use the stable UVC identifier discovered on this Jetson. "auto" falls back
# to any *-video-index0 link and finally /dev/video* when this camera changes.
CAMERA_SOURCE=auto
CAMERA_BY_ID="usb-USB_corp._usb_camera_usb_camera-video-index0"
CAMERA_WAIT_SECONDS=30
CAMERA_BACKEND=auto
CAMERA_FPS=30.0
CAMERA_FRAME_ID=camera_optical_frame
CAMERA_RECONNECT_INTERVAL=2.0
CAMERA_READ_FAILURE_THRESHOLD=3

DEVICE=0
IMGSZ=640
CONF_THRESHOLD=0.25
IOU_THRESHOLD=0.45
PUBLISH_ANNOTATED_IMAGE=true

# Empty uses <project>/outputs/captures. MP4 frames contain detection boxes.
CAPTURE_OUTPUT_DIR=""
RECORD_ON_START=false
RECORDING_FPS=10.0
RECORDING_CODEC=mp4v

# Rebuild only the yolo_detector ROS 2 package before each start. This costs a
# few seconds but ensures a git pull is reflected immediately.
BUILD_ON_START=true

# Open the detector's OpenCV window when a graphical desktop is available.
# Keep false for headless/SSH use; the desktop shortcut passes --view.
OPEN_VIEWER=false
